{
  "name": "pdftion",
  "version": "0.5.4",
  "description": "AI-open PDF annotation, editing, export, and note integration tools.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "check": "tsc --noEmit && node --check main.js",
    "dist": "npm run check && npm run build && node --check main.js",
    "test": "node --test tests/*.test.mjs"
  },
  "keywords": [],
  "author": "Murat",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "docx": "^9.7.1",
    "jszip": "^3.10.1",
    "pdf-lib": "^1.17.1",
    "pptxgenjs": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "esbuild": "^0.28.0",
    "obsidian": "^1.13.0",
    "typescript": "^6.0.3"
  }
}
